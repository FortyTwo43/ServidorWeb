# 🏗️ Diagrama de Arquitectura - Sistema de Seguros

## Arquitectura General Actual

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENTE / FRONTEND                        │
│                    (Web, Mobile, Desktop)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                    HTTP/HTTPS Requests
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      REST API ENDPOINTS                          │
│  ⏳ Controllers (próximos a crear)                              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ POST   /conductores                → crear              │   │
│  │ GET    /conductores                → obtener todos      │   │
│  │ GET    /conductores/:id            → obtener uno        │   │
│  │ PUT    /conductores/:id            → actualizar         │   │
│  │ DELETE /conductores/:id            → eliminar           │   │
│  │ GET    /conductores/buscar/:nombre → buscar             │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ POST   /vehiculos                                       │   │
│  │ GET    /vehiculos                                       │   │
│  │ GET    /vehiculos/:id                                  │   │
│  │ GET    /vehiculos/conductor/:conductorId               │   │
│  │ PUT    /vehiculos/:id                                  │   │
│  │ DELETE /vehiculos/:id                                  │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ GET    /coberturas                                      │   │
│  │ GET    /coberturas/:id                                 │   │
│  │ GET    /coberturas/tipo/:tipo                          │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ POST   /cotizaciones                                    │   │
│  │ GET    /cotizaciones                                    │   │
│  │ GET    /cotizaciones/:id                               │   │
│  │ POST   /cotizaciones/:id/aprobar                       │   │
│  │ POST   /cotizaciones/:id/rechazar                      │   │
│  │ DELETE /cotizaciones/:id                               │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────┬────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│               REST DATA SERVICES (Persistencia)                  │
│                        ✅ Implementado                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ ConductorDataService                                    │   │
│  │  └─ Maneja: conductores en memoria                      │   │
│  │  └─ Métodos: CRUD + buscar + validaciones              │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ VehiculoDataService                                     │   │
│  │  └─ Maneja: vehiculos en memoria                        │   │
│  │  └─ Métodos: CRUD + por conductor + validaciones       │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ CoberturaDataService                                    │   │
│  │  └─ Maneja: coberturas en memoria + default            │   │
│  │  └─ Métodos: CRUD + por tipo + validaciones            │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ CotizacionDataService                                   │   │
│  │  └─ Maneja: cotizaciones en memoria                     │   │
│  │  └─ Métodos: CRUD + estado + validaciones              │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────┬────────────────────────────────────────┘
                          │
           ┌──────────────┴──────────────┐
           │                            │
           ▼                            ▼
┌────────────────────────────┐  ┌──────────────────────────┐
│  DOMAIN SERVICES           │  │ DATOS EN MEMORIA         │
│  (Lógica de Negocio)       │  │ (Persistencia)           │
│      ✅ Implementado       │  │                          │
│                            │  │ ┌────────────────────┐   │
│ ┌──────────────────────┐   │  │ │ conductores: []    │   │
│ │ ConductorService     │───┼──┼→│ vehiculos: []      │   │
│ │ ─────────────────    │   │  │ │ coberturas: []     │   │
│ │ calcularEdad()       │   │  │ │ cotizaciones: []   │   │
│ │ validarExperiencia() │   │  │ └────────────────────┘   │
│ │ validarEmailUnico()  │   │  │                          │
│ │ validarLicencia()    │   │  │ (Luego: Base de Datos)  │
│ │ validarDocumento()   │   │  │ (PostgreSQL/MySQL)      │
│ └──────────────────────┘   │  │                          │
│                            │  └──────────────────────────┘
│ ┌──────────────────────┐   │
│ │ VehiculoService      │   │
│ │ ─────────────────    │   │
│ │ calcularValor()      │   │
│ │ validarPlaca()       │   │
│ │ validarVin()         │   │
│ └──────────────────────┘   │
│                            │
│ ┌──────────────────────┐   │
│ │ CoberturaService     │   │
│ │ ─────────────────    │   │
│ │ calcularPrima()      │   │
│ │ validarCobertura()   │   │
│ └──────────────────────┘   │
│                            │
│ ┌──────────────────────┐   │
│ │ CotizacionService    │   │
│ │ ─────────────────    │   │
│ │ calcularCotizacion() │   │
│ │ validarFechas()      │   │
│ │ cotizacionVencida()  │   │
│ └──────────────────────┘   │
│                            │
└────────────────────────────┘
```

---

## Flujo de una Petición: Crear Conductor

```
1️⃣ Cliente
   ┌───────────────────────────────────────────┐
   │ POST /conductores                         │
   │ {                                         │
   │   nombre: "Juan",                         │
   │   apellido: "Pérez",                      │
   │   email: "juan@test.com",                 │
   │   ...                                     │
   │ }                                         │
   └───────────────────────────────────────────┘
                    │
                    │ HTTP
                    ▼
2️⃣ REST Controller (próximo a crear)
   ┌───────────────────────────────────────────┐
   │ @Post()                                   │
   │ async crear(@Body() dto) {                │
   │   return await this.dataService.crear()   │
   │ }                                         │
   └───────────────────────────────────────────┘
                    │
                    ▼
3️⃣ REST Data Service (ConductorDataService)
   ┌───────────────────────────────────────────┐
   │ crearConductor(dto) {                     │
   │   // Valida usando domain service         │
   │   if (!domain.validarEmailUnico())        │
   │     throw "Email duplicado"               │
   │                                           │
   │   // Crea objeto                          │
   │   const nuevo = {                         │
   │     id: uuid(),                           │
   │     ...dto,                               │
   │     timestamps...                         │
   │   }                                       │
   │                                           │
   │   // Guarda en persistencia               │
   │   this.conductores.push(nuevo)            │
   │                                           │
   │   // Retorna                              │
   │   return mapearADto(nuevo)                │
   │ }                                         │
   └───────────────────────────────────────────┘
      │                            │
      │ Llama método               │ Accede lista
      ▼ del domain                 ▼
   ┌────────────────────┐   ┌──────────────┐
   │ Domain Service     │   │ Persistencia │
   │ validarEmailUnico()│   │ conductores[]│
   │  - Recibe array    │   │              │
   │  - Busca email     │   │ Agrega nuevo │
   │  - Retorna boolean │   │ conductor   │
   └────────────────────┘   └──────────────┘
                    │
                    ▼
4️⃣ REST Controller (responde)
   ┌───────────────────────────────────────────┐
   │ HTTP 201 Created                          │
   │ {                                         │
   │   id: "uuid-123",                         │
   │   nombre: "Juan",                         │
   │   apellido: "Pérez",                      │
   │   email: "juan@test.com",                 │
   │   ...                                     │
   │   fechaCreacion: "2025-11-25T10:30:00"   │
   │ }                                         │
   └───────────────────────────────────────────┘
                    │
                    │ HTTP
                    ▼
5️⃣ Cliente
   ┌───────────────────────────────────────────┐
   │ Recibe respuesta 201 con conductor        │
   │ Actualiza interfaz                        │
   └───────────────────────────────────────────┘
```

---

## Flujo de una Petición: Calcular Cotización

```
1️⃣ Cliente
   ┌────────────────────────────────────────────────┐
   │ GET /cotizaciones/calcular?                    │
   │     vehiculoId=xxx                             │
   │     &coberturaId=yyy                           │
   │     &conductorId=zzz                           │
   └────────────────────────────────────────────────┘
                    │
                    │ HTTP
                    ▼
2️⃣ REST Controller (próximo a crear)
   ┌────────────────────────────────────────────────┐
   │ @Get('calcular')                               │
   │ async calcular(                                │
   │   @Query() vehiculoId, coberturaId, etc       │
   │ ) {                                            │
   │   const cotizacion = await dataService        │
   │     .calcularCotizacion()                      │
   │   return cotizacion                            │
   │ }                                              │
   └────────────────────────────────────────────────┘
                    │
                    ▼
3️⃣ REST Data Service (CotizacionDataService)
   ┌────────────────────────────────────────────────┐
   │ calcularCotizacion(vehiculoId, etc) {          │
   │                                                │
   │   // 1. Obtiene datos de persistencia          │
   │   const vehiculo = this.vehiculos.find(...)    │
   │   const cobertura = this.coberturas.find(...)  │
   │   const conductor = this.conductores.find(...) │
   │                                                │
   │   // 2. Llama a domain para cálculo            │
   │   const prima = await domain.calcular(         │
   │     cobertura.prima,                           │
   │     vehiculo.tipo,    // AUTO, TAXI, etc       │
   │     conductor.experiencia // años              │
   │   )                                            │
   │                                                │
   │   // 3. Crea cotización                        │
   │   const cotizacion = {                         │
   │     id: uuid(),                                │
   │     prima: prima,                              │
   │     vehiculoId, coberturaId, conductorId,     │
   │     ...                                        │
   │   }                                            │
   │                                                │
   │   // 4. Guarda                                 │
   │   this.cotizaciones.push(cotizacion)           │
   │                                                │
   │   // 5. Retorna                                │
   │   return mapearADto(cotizacion)                │
   │ }                                              │
   └────────────────────────────────────────────────┘
      │                    │                    │
      │ Lee datos          │ Lee datos          │ Lee datos
      ▼                    ▼                    ▼
   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
   │ vehiculos[]  │  │ coberturas[] │  │ conductores[]│
   │              │  │              │  │              │
   │ Auto: $45M   │  │ Prima: $1.8M │  │ Años: 15     │
   └──────────────┘  │              │  │              │
                     │              │  │              │
                     │              │  │ Experiencia  │
                     │              │  │ > 5 años     │
                     │              │  │ = -10%       │
                     └──────────────┘  └──────────────┘
                    │
                    │ Llama a domain
                    ▼
   ┌──────────────────────────────────────────┐
   │ Domain Service.calcularCotizacion(        │
   │   primaBase: 1800000,                    │
   │   tipoVehiculo: AUTO,                    │
   │   experiencia: 15                        │
   │ ) {                                       │
   │   let prima = primaBase                  │
   │   // Sin recargo para AUTO                │
   │   // Descuento por experiencia            │
   │   if (experiencia > 5) prima *= 0.9      │
   │   return Math.round(prima)                │
   │ }                                         │
   │                                          │
   │ Retorna: 1,620,000                       │
   └──────────────────────────────────────────┘
                    │
                    │ Retorna al data service
                    ▼
4️⃣ REST Controller (responde)
   ┌────────────────────────────────────────────────┐
   │ HTTP 200 OK                                    │
   │ {                                              │
   │   id: "uuid-456",                              │
   │   prima: 1620000,                              │
   │   vehiculoId: "xxx",                           │
   │   coberturaId: "yyy",                          │
   │   conductorId: "zzz",                          │
   │   estado: "PENDIENTE",                         │
   │   ...                                          │
   │ }                                              │
   └────────────────────────────────────────────────┘
                    │
                    │ HTTP
                    ▼
5️⃣ Cliente
   ┌────────────────────────────────────────────────┐
   │ Recibe cotización calculada                    │
   │ Muestra: "Prima: $1,620,000"                   │
   │ Botón: "Contratar Seguro"                      │
   └────────────────────────────────────────────────┘
```

---

## Capas de la Arquitectura

```
┌────────────────────────────────────────────────────────────┐
│ LAYER 1: Presentation (Cliente)                            │
│ ─────────────────────────────────────────────────────────  │
│ - Web Browser / Mobile App                                 │
│ - Interfaz de usuario                                      │
│ - HTTP/HTTPS                                               │
└────────────────────────────────────────────────────────────┘
                            ▲
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│ LAYER 2: API (REST Controllers) ⏳                         │
│ ─────────────────────────────────────────────────────────  │
│ - HTTP Endpoints                                           │
│ - Validación de entrada (ValidationPipe)                   │
│ - Inyección de dependencias                                │
│ - Manejo de errores (Exception Filters)                    │
│ - Autenticación/Autorización (Guards) ⏳                  │
└────────────────────────────────────────────────────────────┘
                            ▲
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│ LAYER 3: Business Logic (Domain Services) ✅              │
│ ─────────────────────────────────────────────────────────  │
│ - Cálculos (calcular edad, prima, cotización)             │
│ - Validaciones (email único, licencia única)              │
│ - Decisiones (vencida, experiencia mínima)                │
│ - Reglas de negocio                                        │
│ - Sin estado, sin persistencia                             │
└────────────────────────────────────────────────────────────┘
                            ▲
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│ LAYER 4: Data Access (REST Data Services) ✅              │
│ ─────────────────────────────────────────────────────────  │
│ - CRUD Operations                                          │
│ - Persistencia (listas en memoria)                         │
│ - Inyecta domain services                                  │
│ - Orquesta llamadas                                        │
│ - Filtrado y búsqueda                                      │
└────────────────────────────────────────────────────────────┘
                            ▲
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│ LAYER 5: Data Storage (Persistencia) ✅                   │
│ ─────────────────────────────────────────────────────────  │
│ - Listas en memoria (ACTUAL)                              │
│ - Base de datos (FUTURO)                                   │
│ - SQL: PostgreSQL / MySQL                                 │
│ - ORM: TypeORM                                             │
└────────────────────────────────────────────────────────────┘
```

---

## Componentes y Dependencias

```
REST Controller
    │
    ├─ Inyecta → REST Data Service
    │               │
    │               ├─ Inyecta → Domain Service
    │               │               │
    │               │               └─ Métodos puros
    │               │
    │               └─ Accede → Listas en Memoria
    │
    └─ Maneja → HTTP Requests/Responses

GraphQL Resolver (Futuro)
    │
    ├─ Inyecta → REST Data Service
    │
    └─ Maneja → GraphQL Queries/Mutations

WebSocket Gateway (Futuro)
    │
    ├─ Inyecta → REST Data Service
    │
    └─ Maneja → WebSocket Events
```

---

## Estados de Cotización

```
┌─────────────┐
│  PENDIENTE  │  ← Estado inicial
└──────┬──────┘
       │
       ├─ Aprobar       ─→  ┌──────────────┐
       │                    │  APROBADA    │
       │                    └──────────────┘
       │
       ├─ Rechazar      ─→  ┌──────────────┐
       │                    │  RECHAZADA   │
       │                    └──────────────┘
       │
       └─ Vencer        ─→  ┌──────────────┐
                            │  VENCIDA     │
                            └──────────────┘
```

---

## Matriz de Responsabilidades

| Responsabilidad | Domain | REST Data | REST Controller |
|----------------|--------|-----------|-----------------|
| Calcular | ✅ | ✗ | ✗ |
| Validar Negocio | ✅ | ✗ | ✗ |
| Validar Persistencia | ✗ | ✅ | ✗ |
| CRUD | ✗ | ✅ | ✗ |
| Manejo HTTP | ✗ | ✗ | ✅ |
| Autenticación | ✗ | ✗ | ✅ |
| Persistencia | ✗ | ✅ | ✗ |
| Retornar DTOs | ✗ | ✅ | ✅ |

---

**Arquitectura clara, mantenible y escalable** 🎯

